import { classNames } from '../utils/classNames';

function Home() {
  return (
    <main className={classNames('w-screen h-screen bg-gray-100')}>
      <h1>Home</h1>
    </main>
  );
}

export default Home;
